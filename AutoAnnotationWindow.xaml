<Window x:Class="YoloAnnotator.AutoAnnotationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI 自动标注助手" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen" 
        Background="{StaticResource BackgroundBrush}"
        WindowStyle="None">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="{x:Static SystemParameters.WindowResizeBorderThickness}" />
    </WindowChrome.WindowChrome>
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="AppResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 自定义标题栏 -->
        <Border Grid.Row="0" Background="{StaticResource TitleBarBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 标题 -->
                <TextBlock Grid.Column="0" Text="自动标注助手" FontWeight="SemiBold" FontSize="12"
                          VerticalAlignment="Center" Margin="12,0" Foreground="{StaticResource TextPrimaryBrush}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsActive, RelativeSource={RelativeSource AncestorType=Window}}" Value="False">
                                    <Setter Property="Foreground" Value="#666666"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- 窗口控制按钮 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button Name="BtnMinimize" Style="{StaticResource TitleBarButtonStyle}" Click="BtnMinimize_Click">
                        <Path Data="M0,8 L16,8" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.5"
                              Stretch="Uniform" Width="10" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                    <Button Name="BtnMaximize" Style="{StaticResource TitleBarButtonStyle}" Click="BtnMaximize_Click">
                        <Path Data="M0,0 L12,0 L12,12 L0,12 Z" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.5"
                              Stretch="Uniform" Width="10" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                    <Button Name="BtnRestore" Style="{StaticResource TitleBarButtonStyle}" Click="BtnMaximize_Click" Visibility="Collapsed">
                        <Path Data="M0,2 L12,2 L12,14 L0,14 Z M2,2 L2,0 L14,0 L14,12 L12,12" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.5"
                              Stretch="Uniform" Width="10" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                    <Button Name="BtnClose" Style="{StaticResource TitleBarCloseButtonStyle}" Click="BtnClose_Click">
                        <Path Data="M0,0 L12,12 M12,0 L0,12" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.5"
                              Stretch="Uniform" Width="10" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 顶部工具栏 -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" 
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- 左侧参数设置 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="任务类型:" VerticalAlignment="Center" Margin="0,0,10,0" 
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <ComboBox x:Name="CmbTaskType" Width="150" SelectedIndex="0">
                        <ComboBoxItem Content="目标检测"/>
                        <ComboBoxItem Content="实例分割"/>
                        <ComboBoxItem Content="姿态估计"/>
                        <ComboBoxItem Content="旋转框"/>
                    </ComboBox>

                    <TextBlock Text="模型大小:" VerticalAlignment="Center" Margin="20,0,10,0" 
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <ComboBox x:Name="CmbModel" Width="100" SelectedIndex="0">
                        <ComboBoxItem Content="n (nano)"/>
                        <ComboBoxItem Content="s (small)"/>
                        <ComboBoxItem Content="m (medium)"/>
                        <ComboBoxItem Content="l (large)"/>
                        <ComboBoxItem Content="x (xlarge)"/>
                    </ComboBox>

                    <TextBlock Text="置信度:" VerticalAlignment="Center" Margin="20,0,10,0" 
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <Slider x:Name="SliderConfidence" Width="120" Minimum="0.1" Maximum="1.0" Value="0.25" 
                            VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ElementName=SliderConfidence, Path=Value, StringFormat={}{0:F2}}" 
                               VerticalAlignment="Center" Margin="10,0,0,0" Width="35"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    
                    <TextBlock Text="IOU:" VerticalAlignment="Center" Margin="20,0,10,0" 
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <Slider x:Name="SliderIOU" Width="120" Minimum="0.1" Maximum="1.0" Value="0.7" 
                            VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ElementName=SliderIOU, Path=Value, StringFormat={}{0:F2}}" 
                               VerticalAlignment="Center" Margin="10,0,0,0" Width="35"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                </StackPanel>
                
                <!-- 右侧操作按钮 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="BtnAutoAnnotate" Content="🤖 开始标注" Width="120" Height="32" 
                            Style="{StaticResource PrimaryButtonStyle}" Click="BtnAutoAnnotate_Click"/>
                    <Button x:Name="BtnBatchAnnotate" Content="📦 批量标注" Width="120" Height="32" 
                            Style="{StaticResource SecondaryButtonStyle}" Click="BtnBatchAnnotate_Click" 
                            Margin="10,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主内容区域 -->
        <Grid Grid.Row="2" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="320"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：图片列表 -->
            <Border Grid.Column="0" Background="{StaticResource SidebarBrush}" 
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- 图片列表标题 -->
                    <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" 
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="12,10">
                        <TextBlock Text="图片列表" FontWeight="SemiBold" FontSize="13"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                    </Border>
                    
                    <!-- 图片列表 -->
                    <ListBox x:Name="LstImages" Grid.Row="1" Background="Transparent" 
                             BorderThickness="0" SelectionChanged="LstImages_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12,8" Background="Transparent">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Name}" 
                                                   TextTrimming="CharacterEllipsis" 
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Status}" 
                                                   FontSize="10" Margin="8,0,0,0"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- 中间：图片预览 -->
            <Border Grid.Column="1" Background="{StaticResource BackgroundBrush}">
                <Grid>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <Grid>
                            <Image x:Name="ImgPreview" Stretch="Uniform" HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                            <Canvas x:Name="AnnotationCanvas" HorizontalAlignment="Center" 
                                    VerticalAlignment="Center"/>
                        </Grid>
                    </ScrollViewer>
                    
                    <!-- 无图片提示 -->
                    <TextBlock x:Name="TxtNoImage" Text="请选择图片" 
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               FontSize="16" Foreground="{StaticResource TextSecondaryBrush}"
                               Visibility="Visible"/>
                </Grid>
            </Border>

            <!-- 右侧：检测结果 -->
            <Border Grid.Column="2" Background="{StaticResource SidebarBrush}" 
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- 检测结果标题 -->
                    <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" 
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="12,10">
                        <Grid>
                            <TextBlock Text="检测结果" FontWeight="SemiBold" FontSize="13"
                                       Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            <TextBlock x:Name="TxtDetectionCount" Text="0 个对象" 
                                       HorizontalAlignment="Right" FontSize="11"
                                       Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    
                    <!-- 检测结果列表 -->
                    <ListBox x:Name="LstDetections" Grid.Row="1" Background="Transparent" 
                             BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12,8" Background="Transparent" 
                                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Text="{Binding ClassName}" 
                                                   FontWeight="SemiBold" FontSize="12"
                                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock Text="置信度: " FontSize="10"
                                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                                            <TextBlock Text="{Binding Confidence, StringFormat={}{0:P1}}" 
                                                       FontSize="10" FontWeight="SemiBold"
                                                       Foreground="{StaticResource AccentBrush}"/>
                                            <TextBlock Text=" | " FontSize="10" Margin="6,0"
                                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                                            <TextBlock Text="{Binding Type}" FontSize="10"
                                                       Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    
                    <!-- 操作按钮 -->
                    <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" 
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="12">
                        <StackPanel>
                            <Button x:Name="BtnSaveCurrent" Content="💾 保存当前" Height="32" 
                                    Style="{StaticResource PrimaryButtonStyle}" Click="BtnSaveCurrent_Click"
                                    IsEnabled="False"/>
                            <Button x:Name="BtnClearCurrent" Content="🗑️ 清除结果" Height="32" Margin="0,8,0,0"
                                    Style="{StaticResource SecondaryButtonStyle}" Click="BtnClearCurrent_Click"
                                    IsEnabled="False"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <!-- 底部状态栏 -->
        <Border Grid.Row="3" Background="{StaticResource SurfaceBrush}" 
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="15,8">
            <Grid>
                <TextBlock x:Name="TxtStatus" Text="就绪" 
                           Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock x:Name="TxtProgress" Text="" Margin="0,0,15,0"
                               Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                    <ProgressBar x:Name="ProgressBar" Width="200" Height="4" 
                                 Minimum="0" Maximum="100" Value="0" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
